org.gradle.jvmargs=-Xmx4g -XX:MaxMetaspaceSize=512m
kotlin.daemon.jvmargs=-Xmx4g
android.useAndroidX=true
android.nonTransitiveRClass=true

# Single source of truth for app version (profile + RuStore + CI APK).
# Bump via release-apk skill before publishing.
VERSION_NAME=1.0.16
VERSION_CODE=10016
